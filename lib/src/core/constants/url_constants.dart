/// It's a class that contains constants
class UrlContants {
  /// It's a constant that contains the url of the api.
  static const playlistTracks = 'playlist_tracks/';

  /// It's a constant that contains the url of the api.
  static const playlist = 'playlist/';
}

/// It's a class that contains static constants that are used to
/// reference SVG assets
class SVGConstants {
  /// It's a static constant that references the SVG asset.
  static const logo = 'assets/images/logo.svg';
}
